<h1>Sign up!</h1>

<%= render partial: "shared/pilot_errors", locals: {pilot: @pilot}%>

<%= form_for(@pilot) do |f| %>
  <%= f.text_field :username, placeholder: "Register with a username!" %>
  <%= f.password_field :password, placeholder: "Choose a password!" %>

  <%= f.submit %>
<%  end %>
